<h1>新規旅行投稿</h1>
<%= form_with model: @post, url: '/posts', local:true do |f| %>
  <h4>旅行先</h4>
  <%= f.text_field :location %>
  <h4>コメント</h4>
  <%= f.text_area :text %>
  <%= f.submit '投稿' %>
<% end %>
<%= link_to '戻る', posts_path %>