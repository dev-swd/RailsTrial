<h1>旅行詳細</h1>
<h4>旅行先</h4>
<p><%= @post.location %></p>
<h4>コメント</h4>
<p><%= @post.text %></p>
<%= link_to '編集する', edit_post_path(@post.id) %><br>
<%= button_to '削除する', post_path(@post.id), method: :delete, data: {confirm: "削除しますか？"} %><br>
<%= link_to '投稿一覧ページへ', posts_path %>